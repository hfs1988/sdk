package main

import (
	"sdk/adapters/db"

	_ "github.com/lib/pq"
)

type DB interface {
	Connect() error
}

// const (
// 	host     = "localhost"
// 	port     = 5432
// 	user     = "postgres"
// 	password = "root"
// 	dbname   = "postgres"
// )

const (
	uri = "mongodb://localhost:27017"
)

func main() {
	// var database DB = db.GetPostgresInstance(host, port, user, password, dbname)
	var database DB = db.GetMongoDBInstance(uri)
	err := database.Connect()
	if err != nil {
		panic(err)
	}
}
